# TODO

- [x] Update `Frontend/src/style.css` with paper/line styles + print-friendly rules.
- [x] Update `Frontend/src/App.jsx` to render the complete Handwriting Sample Collection Form UI/UX (sections, radios, fillable lines).
- [x] Run `npm run dev` in `Frontend` to verify the UI loads.
- [x] Run `npm run build` in `Frontend` to ensure no build errors.


